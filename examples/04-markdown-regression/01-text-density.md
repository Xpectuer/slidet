# Markdown 回归样例

这组样例故意把常见讲稿元素放在一起，目的是观察 parser 是否会把它们稳定拼成可读文本。

## 为什么这组内容重要

第一，真实讲稿通常不会只有一段短文本。

第二，它们会混合行内代码，比如 `cargo run -- slides/`，也会混合强调词，比如**加载器**、**解析器**和**降级路径**。

第三，当段落变长时，浏览模式需要靠滚动保持可读性，而不是把结构打散。

---

最后一段用于检查分隔线前后的文本是否还能被稳定保留。
